/**
 * 1. Get board id
 * Add .json to the end of board url and open it in browser. I would recommend you JSON viewer extension.
 * Find out board id from the board dump. for example, actions[0].data.board.id
 * 2. Get api token
 * https://trello.com/app-key
 * 3. Get appropriate token
 * https://trello.com/1/authorize?expiration=never&name=trello-show&response_type=token&key={API_KEY}&scope=read,write,account
 * 4. Get list id
 * https://trello.com/1/boards/{BOARD_ID}/lists
 * Search by list name and take id from the list.
 */

export const BOARD_ID = '';
export const LIST_ID = '';
export const API_KEY = '';
export const API_TOKEN = '';

// instagram
export const CLIENT_ID = '';
export const CLIENT_SECRET = '';
export const ACCESS_TOKEN = '';


